<%= form_for(@profile_dropdown_value) do |f| %>
  <% if @profile_dropdown_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile_dropdown_value.errors.count, "error") %> prohibited this profile_dropdown_value from being saved:</h2>

      <ul>
      <% @profile_dropdown_value.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :profile_id %><br />
    <%= f.number_field :profile_id %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
