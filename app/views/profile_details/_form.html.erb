<%= form_for(@profile_detail) do |f| %>
  <% if @profile_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile_detail.errors.count, "error") %> prohibited this profile_detail from being saved:</h2>

      <ul>
      <% @profile_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :learner_id %><br />
    <%= f.number_field :learner_id %>
  </div>
  <div class="field">
    <%= f.label :profile_id %><br />
    <%= f.number_field :profile_id %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
