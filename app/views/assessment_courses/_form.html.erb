<%= form_for(@assessment_course) do |f| %>
  <% if @assessment_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assessment_course.errors.count, "error") %> prohibited this assessment_course from being saved:</h2>

      <ul>
      <% @assessment_course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :assessment_id %><br />
    <%= f.number_field :assessment_id %>
  </div>
  <div class="field">
    <%= f.label :course_id %><br />
    <%= f.number_field :course_id %>
  </div>
  <div class="field">
    <%= f.label :package_id %><br />
    <%= f.number_field :package_id %>
  </div>
  <div class="field">
    <%= f.label :assessment_package_id %><br />
    <%= f.number_field :assessment_package_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
