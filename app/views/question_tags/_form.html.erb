<%= form_for(@question_tag) do |f| %>
  <% if @question_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_tag.errors.count, "error") %> prohibited this question_tag from being saved:</h2>

      <ul>
      <% @question_tag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %><br />
    <%= f.number_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :tagvalue_id %><br />
    <%= f.number_field :tagvalue_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
